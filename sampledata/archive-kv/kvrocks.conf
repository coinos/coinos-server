bind 0.0.0.0
port 6379
workers 4
dir /data

# Low memory footprint for archival workload
rocksdb.block_cache_size 256
rocksdb.compression zstd
rocksdb.wal_size_limit_mb 256

# Log to stdout for Docker
log-dir stdout
